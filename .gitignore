# secrets — never commit
.env
.env.local
.env.*.local

# deps
node_modules/
web/node_modules/

# build output
dist/
web/.next/
web/out/

# sqlite runtime db (regenerated by settle.ts)
db/*.sqlite
db/*.sqlite-shm
db/*.sqlite-wal
*.sqlite

# logs / misc
*.log
.DS_Store
.vitest/
coverage/

# Playwright E2E artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Lighthouse CI
.lighthouseci/

# NOTE: assets/ is tracked brand work (icons + animated README hero) — do NOT ignore it.

# internal build scaffolding (not for public repo)
_BUILD_BRIEF.md
